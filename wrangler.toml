# Cloudflare Workers – static SPA (no Vite plugin required)
# Build first: npm run build  →  then: wrangler deploy
name = "ddmaxxpet"
compatibility_date = "2024-01-01"

[assets]
directory = "dist"
not_found_handling = "single-page-application"
